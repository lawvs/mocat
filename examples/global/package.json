{
  "name": "global-example",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --serveStatic '../../packages/mocat/build/' --no-open --no-ui --port 3002 --watch",
    "build": "mkdir -p dist && cp -r index.html api ../../packages/mocat/build/index.js dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "browser-sync": "^2.26.12"
  },
  "dependencies": {
    "mocat": "^0.3.0"
  }
}
