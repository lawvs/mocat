{
  "name": "global-example",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --serveStatic ./node_modules/mocat/build/ --watch",
    "build": "mkdir -p dist && cp -r index.html api ./node_modules/mocat/build/index.js dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "browser-sync": "^2.26.12"
  },
  "dependencies": {
    "mocat": "^0.2.0"
  }
}
